<div id="floatingPass" class="floating mx-auto w-md-50 d-none flex-nowrap justify-content-center position-fixed">
    <div class="card limit w-100 mx-md-5 w-md-100 shadow-lg border-0 rounded-lg">
        <div class="card-header">
            <h3 class="text-center font-weight-light my-4">Reestablecer contraseña</h3>
            <button id="closeReg" onclick="toggleFloating(0)"
                class="h3 m-0 p-1 w-auto position-absolute form-control btn-danger">
                <i class="fas fa-times-circle fa-2x"></i>
            </button>
        </div>
        <div class="card-body">
            <form method="POST" name="f_pass" id="f_pass">
                <div class="form-floating mb-3">
                    <input class="form-control" id="_id_u" name="_id" type="text" placeholder="." />
                    <label for="_id_u">Usuario</label>
                </div>
                <div class="d-flex align-items-center justify-content-between mt-4 mb-0">
                    <a class="small" href="javascript:toggleFloating(1)">Regresar a Incio de Sesión</a>
                </div>
                <div class="d-flex align-items-center justify-content-between mt-4 mb-0">
                    <p class="small"></p>
                    <button class="btn btn-primary" type="button" onclick="password()">Generar ticket</button>
                </div>
            </form>
        </div>
        <div class="card-footer text-center py-3">
        </div>
    </div>
</div>